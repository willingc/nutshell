# Coding Standards

- Always add `type` hints to `function` parameters
- Write docstrings in Sphinx format
- Create unit tests for new functions in `test/` directory
- Follow functional programming principles where possible
